rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow read/write for all collections to support seamless syncing without Firebase Auth
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
